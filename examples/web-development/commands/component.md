Create a React/TypeScript component.

## Boundary (CONSTRAINT)

- Output must be a .tsx file
- No `any` type annotations
- Must export the component as default or named export

  ### Aspire within boundary (ASPIRATION)

  All aspirations below must not violate the boundary above.

  - Clean component architecture (single responsibility)
  - Proper TypeScript types (no implicit any)
  - Accessible (ARIA attributes where needed)
  - Responsive layout

    #### Decide freely (FREEDOM)

    Choices below are within the boundary and aspiration direction.

    - CSS approach (Tailwind, CSS modules, styled-components)
    - State management method
    - File organization (co-located tests, styles)
